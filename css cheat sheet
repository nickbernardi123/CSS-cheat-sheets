<!-- File: css-cheatsheet.md -->
# CSS Cheat Sheet

## Basics
- **Syntax**: `selector { property: value; }`  
- **Including CSS**  
  - External: `<link rel="stylesheet" href="styles.css">`[4]  
  - Internal: `<style>…</style>` in `<head>`  
  - Inline: `<div style="…">`  
- **At-Rules**:  
  - `@import url("styles.css");` to pull in another stylesheet[1]  
  - `@charset "UTF-8";`

## Selectors
- **Type**: `div`, `p`  
- **Class**: `.classname`  
- **ID**: `#elementId`  
- **Universal**: `*`  
- **Attribute**: `[attr]`, `[attr="value"]`  
- **Combinators**:  
  - Descendant: `ul li`  
  - Child: `ul > li`  
  - Adjacent sibling: `h1 + p`  
  - General sibling: `h1 ~ p`  
- **Pseudo-classes**: `:hover`, `:active`, `:focus`, `:nth-child()`, etc.  
- **Pseudo-elements**: `::before`, `::after`, `::first-letter`, etc.

## Box Model
- **Content**: actual width/height  
- **Padding**: inside spacing  
- **Border**: line around padding  
- **Margin**: outside spacing  
- `box-sizing: content-box | border-box;`

## Typography
- `font-family`, `font-size`, `font-weight`, `font-style`  
- `line-height`, `letter-spacing`, `word-spacing`  
- `text-align`, `text-transform`, `text-decoration`, `text-shadow`

## Colors & Backgrounds
- **Color Formats**:  
  - HEX: `#RRGGBB`, `#RGB`  
  - RGB(A): `rgb(255,0,0)`, `rgba(255,0,0,0.5)`  
  - HSL(A): `hsl(120,50%,50%)`, `hsla(120,50%,50%,0.3)`  
- **Background Properties**:  
  - `background-color`, `background-image`, `background-position`  
  - `background-size`, `background-repeat`, `background-attachment`  
- **Gradient**:  
  - `linear-gradient(direction, color-stop1, color-stop2,…)`  
  - `radial-gradient(shape size at position, color-stop,…)`  
  Tools like HTMLCheatSheet’s gradient generator provide live previews[1].

## Borders & Shadows
- **Borders**: `border: width style color;`  
  - Shorthand for `border-top`, `border-right`, etc.  
- **Border Radius**: `border-radius: all-corners` or `border-top-left-radius`[1]  
- **Box Shadow**: `box-shadow: x-offset y-offset blur spread color inset?`[1]  
- **Text Shadow**: `text-shadow: x-offset y-offset blur color`

## Transforms & Transitions
- **Transforms**: `transform: translate(), rotate(), scale(), skew()`[1]  
- **Transitions**:  
